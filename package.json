{
    "name": "cirro-monorepo",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "test": "pnpm -r test",
        "test:coverage": "pnpm -r test:coverage"
    },
    "workspaces": [
      "packages/*",
      "examples/*"
    ]
}
